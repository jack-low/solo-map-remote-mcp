services:
  comparison-engine:
    build: .
    ports:
      - "127.0.0.1:${SOLO_MAP_PUBLIC_PORT:-8000}:8000"
    environment:
      SOLO_MAP_API_BASE_URL: "http://localhost:8000/v1"
      SOLO_MAP_API_TOKEN: "${SOLO_MAP_API_TOKEN:?SOLO_MAP_API_TOKEN is required}"
